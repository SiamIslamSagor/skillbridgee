/** Elements that trigger the cursor's hover/press state. */
export const INTERACTIVE_SELECTOR =
  'button, a, input, textarea, select, [role="button"], [data-cursor]';

/** Elements that the magnetic engine pulls toward the pointer. */
export const MAGNETIC_SELECTOR = ".magnetic, [data-magnetic]";
